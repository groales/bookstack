# === VARIABLES REQUERIDAS ===
# URL final de acceso a BookStack
# Ejemplo local:  http://localhost:6875
# Ejemplo con proxy: https://bookstack.midominio.com
APP_URL=http://localhost:6875

# Generar con: docker run -it --rm --entrypoint /bin/bash lscr.io/linuxserver/bookstack appkey
APP_KEY=''

# Generar PASSWORDS con: openssl rand -base64 32
DB_PASSWORD=''
ROOT_PASSWORD=''

# === CONFIGURACIÓN OPCIONAL ===
# Base de datos (valores por defecto)
DB_NAME=bookstack
DB_USER=bookstack